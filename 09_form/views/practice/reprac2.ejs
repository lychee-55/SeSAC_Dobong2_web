<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>실습문제 2</title>
  </head>
  <body>
    <h1>실습문제2</h1>
    <!-- method가 다를 때는 action url이 같아도 상관없음 -->
    <form action="/reprac2_post" method="post">
      <label>
        <!-- 오브젝트의 key값이 name -->
        이름
        <input type="text" name="name" />
      </label>

      <fieldset>
        <legend>성별</legend>
        <label>
          <!-- name을 같이 해야 둘 중 하나 선택 -->
          <!-- value는 서버에 보내야 하는 값을 지정 -->
          <input type="radio" name="gender" value="남자" />
          <span>남자</span>
        </label>
        <label>
          <input type="radio" name="gender" value="여자" />
          <span>여자</span>
        </label>
      </fieldset>

      <fieldset>
        <!-- ejs는 자바스크립트를 사용할수 있음 -->
        <legend>생년월일</legend>
        <select name="year">
          <% for(let i=1980; i<=2010; i++){%>
          <option value="<%=i%>"><%=i%></option>
          <!-- 어떤 값을 넣는 건지 i를 value에 표기 -->
          <% } %>
        </select>
        <span>년</span>
        <select name="month">
          <% for(let i=1; i<=12; i++){%>
          <option value="<%=i%>"><%=i%></option>
          <% } %>
        </select>
        <span>월</span>
        <select name="date">
          <% for(let i=1; i<=31; i++){%>
          <option value="<%=i%>"><%=i%></option>
          <!-- 어떤 값을 넣는 건지 i를 value에 표기 -->
          <% } %>
        </select>
        <span>일</span>
      </fieldset>

      <fieldset>
        <legend>관심사</legend>
        <label>
          <input type="checkbox" name="favorite" value="여행" />
          <span>여행</span>
        </label>
        <label>
          <input type="checkbox" name="favorite" value="패션" />
          <span>패션</span>
        </label>
        <label>
          <input type="checkbox" name="favorite" value="음식" />
          <span>음식</span>
        </label>
      </fieldset>

      <hr />
      <fieldset>
        <legend>추가 속성들</legend>
        <label>
          좋아하는 색상
          <input type="color" name="color" />
        </label>

        <br />
        <label>
          좋아하는 숫자
          <input type="number" name="number" />
        </label>
        <br />
        <label>
          다짐 한마디
          <!-- 몇줄 ,글자 몇개 -->
          <textarea name="comment" cols="30" rows="10"></textarea>
        </label>
      </fieldset>
      <input type="submit" value="제출" />
    </form>
  </body>
</html>
