export interface TodoItemInterface {
  id: number;
  text: string;
  done: boolean;
}

// Matzip관련
export interface MatzipInterface {
  idx?: number;
  imgSrc: string;
  title: string;
  desc: string;
}
