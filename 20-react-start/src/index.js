import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />); // component를 불러오는 역할을 하고 있다. -- 태그형식으로 불러오고 있음.
// component는 js라이브러리 이기 때문에 확장자이름이 .js임
// component는 PascalCase로 작성하고 있음
